"""
Fruit metadata API routes — placeholder for Phase 5.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["fruits"])

# GET /api/fruits and GET /api/fruits/{class_name} will be implemented in Phase 5
